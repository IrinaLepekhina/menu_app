<%= simple_form_for @category do |f| %>
  <%= f.error_notification %>
  <%= f.input :title%>  

  <%= f.simple_fields_for :meals do |meals| %>
    <%= render 'meal_fields',  { f: meals, category: @category }  %>
  <% end %>

  <%= f.submit 'Create Category', class: 'btn btn-primary' %>
<% end %>

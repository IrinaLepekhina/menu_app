%table
  %tr
    %th= @menu.title
  - @menu.menu_meals.each do |menu_meal|
    %tr
      %td= link_to menu_meal.meal.title
      %td= link_to "edit", edit_menu_meal_path(menu_meal)
      %td= button_to 'delete', menu_meal_path(menu_meal), |
          method: :delete,                               |
          form: {data: {turbo_confirm: 'are you sure?'}} |
%p= link_to "add new meal", new_menu_meal_path
%p
  = link_to "edit", edit_menu_path(@menu)
  |
  \#{link_to "view all", menus_path} |
  \#{button_to 'delete', menu_path(@menu),  method: :delete, data: {confirm: "Are you sure?"}}

<%= simple_form_for @menu_meal do |f| %>
    <%= f.input :menu_id, collection: Menu.all, label_method: :title, value_method: :id, :prompt => 'Choose an existing Item', label: 'Menus' %>
  
    <%= f.submit 'Fill Menu', class: 'btn btn-primary' %>
<% end %>